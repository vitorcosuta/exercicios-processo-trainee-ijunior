function exponentiation(bases, exponent){

    return Math.pow(bases, exponent);
}

let number = 3, exponent = 3;
console.log(`${number} ^ ${exponent} is equal to ${exponentiation(number, exponent)}`);
